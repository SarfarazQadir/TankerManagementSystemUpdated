@* @model TankerManagementSystem.Models.PersonalKhata

@{
    ViewData["Title"] = "Add Credit Entry";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<section class="section">

    <div class="card">

        <div class="card-header">
            <h4>Add Customer Credit Entry</h4>
        </div>

        <div class="card-body">

            <form method="post">

                <div class="form-group">
                    <label>Date</label>

                    <input type="date"
                           name="EntryDate"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label>Description</label>

                    <input type="text"
                           name="Description"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label class="text-success">
                        Credit Add Amount
                    </label>

                    <input type="number"
                           name="AddAmount"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label class="text-danger">
                        Customer Paid Amount
                    </label>

                    <input type="number"
                           name="MinusAmount"
                           class="form-control" />
                </div>

                <button class="btn btn-primary">
                    Save Entry
                </button>
                <a href="@Url.Action("Index")"
                   class="btn btn-secondary">
                    Back
                </a>
            </form>

        </div>

    </div>

</section> *@

@model TankerManagementSystem.Models.PersonalKhata

@{
    ViewData["Title"] = "Add Credit Entry";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<section class="section">

    <div class="card">

        <div class="card-header">
            <h4>Add Customer Credit Entry</h4>
        </div>

        <div class="card-body">

            <form method="post" onsubmit="return validateKhataForm(event)">

                <div class="form-group">
                    <label>
                        Date<span style="color:red;">*</span>
                    </label>

                    <input type="date"
                           id="EntryDate"
                           name="EntryDate"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label>
                        Description<span style="color:red;">*</span>
                    </label>

                    <input type="text"
                           id="Description"
                           name="Description"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label class="text-success">
                        Credit Add Amount
                    </label>

                    <input type="number"
                           id="AddAmount"
                           name="AddAmount"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label class="text-danger">
                        Customer Paid Amount
                    </label>

                    <input type="number"
                           id="MinusAmount"
                           name="MinusAmount"
                           class="form-control" />
                </div>

                <button class="btn btn-primary">
                    Save Entry
                </button>

                <a href="@Url.Action("Index")"
                   class="btn btn-secondary">
                    Back
                </a>

            </form>

        </div>

    </div>

</section>

<script>
    function validateKhataForm(event) {

        let entryDate = document.getElementById("EntryDate").value.trim();
        let description = document.getElementById("Description").value.trim();
        let addAmount = document.getElementById("AddAmount").value.trim();
        let minusAmount = document.getElementById("MinusAmount").value.trim();

        if (entryDate === "") {

            iziToast.error({
                title: 'Error',
                message: 'Date is required!',
                position: 'bottomRight'
            });

            event.preventDefault();
            return false;
        }

        if (description === "") {

            iziToast.error({
                title: 'Error',
                message: 'Description is required!',
                position: 'bottomRight'
            });

            event.preventDefault();
            return false;
        }

        // Dono empty hon to error show karo
        if (addAmount === "" && minusAmount === "") {

            iziToast.error({
                title: 'Error',
                message: 'Please enter Credit or Customer Paid amount!',
                position: 'bottomRight'
            });

            event.preventDefault();
            return false;
        }

        return true;
    }
</script>