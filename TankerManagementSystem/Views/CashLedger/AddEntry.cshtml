@* @model TankerManagementSystem.Models.CashLedger

@{
    ViewData["Title"] = "AddEntry";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}



<section class="section">

    <div class="card">

        <div class="card-header">
            <h4>Add Cash Entry</h4>
        </div>

        <div class="card-body">

            <form method="post">

                <div class="form-group">
                    <label>Date</label>

                    <input type="date"
                           name="EntryDate"
                           class="form-control"
                           required />
                </div>

                <div class="form-group">
                    <label>Description</label>

                    <textarea name="Description"
                              class="form-control"></textarea>
                </div>

                <div class="form-group">
                    <label style="color:green;">Credit</label>

                    <input type="number"
                           step="0.01"
                           name="Credit"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label style="color:red;">Debit</label>

                    <input type="number"
                           step="0.01"
                           name="Debit"
                           class="form-control" />
                </div>

                <button class="btn btn-primary">
                    Save Entry
                </button>

                <a href="@Url.Action("Index")"
                   class="btn btn-secondary">

                    Back

                </a>

            </form>

        </div>

    </div>

</section>
<script>

    @if (TempData["success"] != null)
    {
                <text>

                iziToast.success({
                    title: 'Success',
                    message: '@TempData["success"]',
                    position: 'bottomRight'
                });

                </text>
    }

    @if (TempData["error"] != null)
    {
                <text>

                iziToast.error({
                    title: 'Error',
                    message: '@TempData["error"]',
                    position: 'bottomRight'
                });

                </text>
    }

</script>
 *@
@model TankerManagementSystem.Models.CashLedger

@{
    ViewData["Title"] = "AddEntry";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}

<section class="section">

    <div class="card">

        <div class="card-header">
            <h4>Add Cash Entry</h4>
        </div>

        <div class="card-body">

            <form method="post" onsubmit="return validateCashLedgerForm(event)">

                <div class="form-group">
                    <label>
                        Date<span style="color:red;">*</span>
                    </label>

                    <input type="date"
                           id="EntryDate"
                           name="EntryDate"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label>
                        Description<span style="color:red;">*</span>
                    </label>

                    <textarea id="Description"
                              name="Description"
                              class="form-control"></textarea>
                </div>

                <div class="form-group">
                    <label style="color:green;">
                        Credit
                    </label>

                    <input type="number"
                           step="0.01"
                           id="Credit"
                           name="Credit"
                           class="form-control" />
                </div>

                <div class="form-group">
                    <label style="color:red;">
                        Debit
                    </label>

                    <input type="number"
                           step="0.01"
                           id="Debit"
                           name="Debit"
                           class="form-control" />
                </div>

                <button class="btn btn-primary">
                    Save Entry
                </button>

                <a href="@Url.Action("Index")"
                   class="btn btn-secondary">

                    Back

                </a>

            </form>

        </div>

    </div>

</section>

<script>

    function validateCashLedgerForm(event) {

        let entryDate = document.getElementById("EntryDate").value.trim();
        let description = document.getElementById("Description").value.trim();
        let credit = document.getElementById("Credit").value.trim();
        let debit = document.getElementById("Debit").value.trim();

        if (entryDate === "") {

            iziToast.error({
                title: 'Error',
                message: 'Date is required!',
                position: 'bottomRight'
            });

            event.preventDefault();
            return false;
        }

        if (description === "") {

            iziToast.error({
                title: 'Error',
                message: 'Description is required!',
                position: 'bottomRight'
            });

            event.preventDefault();
            return false;
        }

        // Agar dono empty hon to error show karo
        if (credit === "" && debit === "") {

            iziToast.error({
                title: 'Error',
                message: 'Please enter Credit or Debit amount!',
                position: 'bottomRight'
            });

            event.preventDefault();
            return false;
        }

        return true;
    }

    @if (TempData["success"] != null)
    {
            <text>

                iziToast.success({
                    title: 'Success',
                    message: '@TempData["success"]',
                    position: 'bottomRight'
                });

            </text>
    }

    @if (TempData["error"] != null)
    {
            <text>

                iziToast.error({
                    title: 'Error',
                    message: '@TempData["error"]',
                    position: 'bottomRight'
                });

            </text>
    }

</script>