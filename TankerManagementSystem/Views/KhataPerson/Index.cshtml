@model List<TankerManagementSystem.Models.KhataPerson>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Dashboard.cshtml";
}
@{
    if (TempData["success"] != null)
    {
        <script>
            Swal.fire({
            title: '@TempData["success"]',
            text: 'Clicked the button!',
            icon: 'success'
            });
        </script>

    }
    if (TempData["error"] != null)
    {
        <script>
            Swal.fire({
            title: '@TempData["error"]',
            text: 'Clicked the button!',
            icon: 'error'
            });
        </script>

    }
}

<section class="section">
    <div class="section-body">
        <a href="@Url.Action("Add", "KhataPerson")" class="btn btn-primary align-right">Add Khata Person</a>
        <br /><br />
        <div class="card">
            <div class="card-header">
                <h4>Khata Person Details</h4>
            </div>
            <div class="card-body">
                <div class="table-responsive">

                    <table class="table table-striped table-bordered table-hover" id="myTable">
                        <thead>
                            <tr>
                                <th>S.No</th>
                                <th>Id</th>
                                <th>Name</th>
                                <th>Contact Number</th>
                                <th>Address</th>
                                <th>Description</th>
                                <th>Current Balance</th>
                                <th>CreatedAt</th>
                                <th>CreatedBy</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{
                                int serialNumber = 1;
                            }
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@serialNumber</td>
                                    <td>@item.Id</td>
                                    <td>@item.Name</td>
                                    <td>@item.ContactNumber</td>
                                    <td>@item.Address</td>
                                    <td>@item.Description</td>
                                    <td>@item.CurrentBalance</td>
                                    <td>@item.CreatedAt.ToString("dd/MMM/yyy hh:tt")</td>
                                    <td>@item.CreatedBy</td>
                                    <td>
                                        <div class="dropdown">
                                            <a href="#" data-toggle="dropdown" class="btn btn-warning dropdown-toggle">Options</a>
                                            <div class="dropdown-menu">
                                                <a href="@Url.Action("Statement", "KhataPerson", new { id = item.Id })" class="dropdown-item has-icon">
                                                    <i class="fas fa-eye"></i> View Statement
                                                </a>
                                                <a href="@Url.Action("Edit", "KhataPerson", new { id = item.Id })" class="dropdown-item has-icon">
                                                    <i class="far fa-edit"></i> Edit
                                                </a>
                                                <a href="@Url.Action("Delete", "KhataPerson", new { id = item.Id })" class="dropdown-item has-icon">
                                                    <i class="far fa-trash-alt"></i> Delete
                                                </a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                                serialNumber++;
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>